_default_:
  fetch_by_id      : "/schemas/ids/{id}"                   # GET
  fetch_by_subject : "/subjects/{subject}/versions/latest" # GET
  register         : "/subjects/{subject}/versions"        # POST

