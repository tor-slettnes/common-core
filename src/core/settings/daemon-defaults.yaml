# -*- yaml -*-
#===============================================================================
## @file daemon-defaults.yaml
## @brief Default settings shared across server applications
## @author Tor Slettnes
#===============================================================================

## These settings take precedence over those in `defaults.yaml` for
## daemon/server applications.

log sinks:
  "stderr":
    threshold: INFO

  "syslog":
    threshold: INFO
