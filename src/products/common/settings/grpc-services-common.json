// -*- javascript -*-
//==============================================================================
/// @file common-grpc-services.json
/// @brief gRPC service settings for core applications
/// @author Tor Slettnes <tor@slett.net>
//==============================================================================

{
    // Default service settings for common applications. In addtiion to these,
    // settings for product-specific applications can be found in
    // `grpc-services-PRODUCT_NAME.json`, where PRODUCT_NAME is provided at
    // build time.
    //
    // For now, the following attributes are defined per service:
    //  - "host" and "port" settings for clients, specifying where to connect
    //  - "interface" and "port" for servers, specifying the listener address.
    //
    // The syntax for the service address provided to both the C++ and Python
    // gRPC wrappers is as follows:
    //   [PERSONALITY@][HOST][:PORT]
    //
    // * If HOST and/or PORT is explicitly provided it takes precendence.
    // * If either is missing, PERSONALITY is used to lookup the corresponding
    //   value from a matching profile in the "personalities" section, below.
    // * If still missing, default values per service name are provided in
    //   the "defaults" section.
    // * If all else fails, the compile-time default is "localhost:8080" for
    //   clients, and "[::]:8080" for servers.

    "personalities" :
    {
    },

    "defaults" :
    {
        // Service names extracted from .proto files
        "Demo":
        {
            // "interface" : "",       // Listen on all interfaces by default
            "port"      : 3366      // D-E-M-O on T9 keypad
        }
    }
}
