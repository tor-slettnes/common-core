// -*- javascript -*-
//==============================================================================
/// @file grpc-services-demoproduct.json
/// @brief gRPC service settings for applications included in `demoproduct`
/// @author Tor Slettnes <tor@slett.net>
//==============================================================================

{
    // Default gRPC endpoints for applications specific to `demoproduct`.
    // In addition to these, default settings for common applications as well
    // as more detailed documentation can be found in `grpc-services-common.json`.

    // Service names extracted from .proto files
    "Demo":
    {
        // "interface" : "",       // Listen on all interfaces by default
        "port"      : 3366      // D-E-M-O on T9 keypad
    }
}
