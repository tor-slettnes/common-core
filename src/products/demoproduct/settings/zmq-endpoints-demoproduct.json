// -*- javascript -*-
//==============================================================================
/// @file zmq-endpoints-common.json
/// @brief Connectivity endpoints for ZeroMQ sockets
/// @author Tor Slettnes <tor@slett.net>
//==============================================================================

{
    // Default ZeroMQ endpoints for applications specific to `demoproduct`.
    // In addition to these, default settings for common applications as well
    // as more detailed documentation can be found in `zmq-endpoints-common.json`.

    "Demo Service":         // Endpoint name
    {
        // "scheme" : "tcp",       // Communicate over TCP by default
        // "connect": "localhost", // client connects to localhost
        // "listen" : "*",         // server listens on all interfaces
        "port"   : 33662        // "D-E-M-O-C" on phone keypad
    },

    "Demo Publisher":       // Endpoint name
    {
        // "scheme" : "tcp",       // Communicate over TCP by default
        // "connect": "localhost", // subscriber connects to localhost
        // "listen" : "*",         // publisher listens on all interfaces
        "port"   : 33667,        // D-E-M-O-P on phone keypad
        "format" : "protobuf",   // Payload format
        "filter" : ""            // ZMQ subscription filter     }
}
