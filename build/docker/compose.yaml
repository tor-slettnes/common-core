name: "common-core"

services:
  runtime:
    hostname: "common-core-runtime"

    # ports:
    #   - "3366:3366"             # D-E-M-O (demoserver)
    #   - "7528:7528"             # P-L-A-T (platformserver)
    #   - "7948:7948"             # S-W-I-T (switchboard)
    #   - "6564:6564"             # M-L-O-G (multilogger)

    build:
      args:
        USERNAME: ${USER:-builder}
        UID: ${UID:-1000}

    volumes:
      - type: bind
        source: ${HOME:-/home/${USER:-builder}}
        target: /home/${USER:-builder}
